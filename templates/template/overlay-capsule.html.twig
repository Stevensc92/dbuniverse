<div class="overlay--data">
    <div class="overlay--title">Détails</div>
    <div class="overlay--content">
    {% for stat, value in statCaps %}
        {% set breaker = '' %}
        {% if loop.index % 2 == '0' %}
            {% set breaker = 'overlay--break' %}
        {% endif %}
        <div class="overlay--line {{ breaker }}">
            <span class="stat-name">{{ stat }}</span> : <span class="stat-value">{{ value }}</span>
        </div>
{#        {{ stat }} : <em>{{ value }}</em><br>#}
    {% endfor %}
    </div>
</div>