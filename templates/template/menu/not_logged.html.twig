<nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-between" id="menu">
    <a class="" href="{{ path('index') }}" id="logo"></a>

    <span class="info-player-online">Joueur(s) online : <span>5</span></span>

    <form method="post" action="{{ path('login') }}" class="form-inline">
        <input type="text" class="form-control mb-2 mr-2 mt-2" name="_username" id="username" placeholder="Pseudo"/>
        <input type="password" class="form-control mb-2 mr-2 mt-2" name="_password" id="password" placeholder="Password"/>
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" >
        <button class="form-control btn btn-outline-success mb-2 mt-2" type="submit">Login</button>
    </form>
</nav>